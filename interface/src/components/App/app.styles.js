const styles = {
  root: {
    width: '100%',
    height: '100%',
    display: 'flex',
    flexDirection: 'column',
    alignContent: 'center',
    textAlign: 'center',
    overflow: 'auto',
    flexShrink: 0,
  },
};

export default styles;
